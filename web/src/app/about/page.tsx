export default function About(){return <section className="prose"><p className="eyebrow">BE CHEAP. DON’T BE A CHEAT.</p><h1>A scoreboard.<br/><em>Not an audit.</em></h1><h2>App-synced, honestly labelled</h2><p>Rankings will use reported, settled usage from opted-in cheapoS installations. Installation signatures help detect changed submissions and duplicate uploads. They do not prove that a modified app is reporting genuine inference.</p><h2>Every zero-cost token counts</h2><p>One score adds free remote, included-access, and local-model tokens. Zero-cost means no additional API charge; hardware, electricity, and subscription costs are outside this score. Paid or unknown usage, estimates, and imported history stay out of rankings. Monthly rankings use UTC.</p><h2>Private by default</h2><p>No prompts, source code, project paths or provider credentials belong in a leaderboard upload. Model names are optional: choose whether to send them from cheapoS and display them on your profile. Posting a project is a separate choice.</p><h2>Still taking shape</h2><p>X and GitHub sign-in and private member settings are available once configured. Installation pairing and signed usage sync are available once configured. Community posting and account deletion controls are still being built.</p></section>}
