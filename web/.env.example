# Server-side read-only connection. Never put service-role credentials here.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Trusted public origin used for authentication redirects.
SITE_URL=http://localhost:5188

# Server-only installation RPC. NEVER prefix this variable NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
